import { AccordionTypes } from '../../types/AccordionTypes';

interface AccordionProps {
    widget: AccordionTypes[];
}
const Accordion = ({ widget }: AccordionProps) => {
    return <>accordion</>;
};

export default Accordion;
